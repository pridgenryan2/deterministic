name: Build
on:
    pull_request:
    push:
        branches:
            - main
jobs:
  nodejs-build:
    name: Node.js Build
    runs-on: ubuntu-latest
    steps:
      # ...
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2

      # run any `bun` or `bunx` command
      - run: bun install
      - run: bun test